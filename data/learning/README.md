# Learning observations

`observations/` is an append-only public record of settled, source-backed forecast evaluations. Do not add a row until the exact weekly state, Kalshi close and binary settlement outcome are known. Personal fills, entry prices, positions and notes never belong here.
