# Immutable weekly states

Each `weekly-*.json` file freezes the exact public evidence, forecast, Kalshi snapshot, readiness audit and policies for one preseason or game-week checkpoint. Files are content-addressed and never overwritten. `manifests/` preserves the selected current-state manifest that existed when each checkpoint was frozen.
